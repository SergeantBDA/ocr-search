# Example environment variables for ocr-search — copy to .env and update values

ENV=dev

# Application host and port
APP_HOST=0.0.0.0
APP_PORT=8000

# PostgreSQL connection (SQLAlchemy style using psycopg)
# Replace user, password, host, port and dbname
DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5432/ocr
OUTPUT_ORIGINALS_DIR=D:/SHARE/_OCR_ORIGIN
OUTPUT_TEXTS_DIR=D:/SHARE/_OCR_TEXT